# Instances

No separate data or instance files were found in the imported raw material.

The available instances appear embedded directly in the GAMS source files through job sets, machine sets, parameters, and tables such as `P(i,j)`, `pre(i,o)`, `cjl(j,l)`, and `V(i,o)`.

Future cleanup may extract these embedded instances into structured data files, but that has not been done yet.
