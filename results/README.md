# Results

Some reported thesis/presentation values have been transcribed under `reported_from_thesis/`. These are reported summaries only and have not been independently reproduced. The raw thesis DOCX/PPTX files used during private extraction are not included in this public branch.

No standalone raw solver outputs, logs, `.lst`, `.gdx`, `.xlsx`, `.csv`, or exported result artifacts are included yet. The code was not rerun during repository cleanup.

The `raw/` and `processed/` directories are reserved for future verified outputs and processed result summaries.
